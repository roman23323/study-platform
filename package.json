{
  "name": "study-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd backend && npm ci",
    "build": "cd backend && npm run build",
    "start": "cd backend && node dist/index.js"
  }
}